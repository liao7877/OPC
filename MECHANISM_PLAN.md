# OPC 机制扩展设计方案（2026-08-28 一问一答定稿）

> **定位**：在 C001 现有底座上的机制扩展设计。全部决策经与廖哥逐条问答确认（P18），本文档为唯一权威存档。
> **性质**：设计方案（做什么、为什么），非实施记录。落地时按「六、实施顺序」逐项执行，改必测（P16）、变更可逆（P24）。
> **前置**：`PRINCIPLES.md`（24 条原则）、`AGENT_ECOSYSTEM.md`（平台接入）、`C001/company.md`（公司总纲）。

---

## 〇、决策总览

| # | 议题 | 决策 |
|---|---|---|
| 1 | 需求分析师角色 | 先给总管挂 `demand-clarify` skill，roster/模板预留升级线 |
| 2 | 并发会话机制 | 工位卡（seat）+ 持卡会话 + memory inbox 合并，四场景全覆盖 |
| 3 | 前置阻塞 | `blocked_by` 字段 + 员工自查 + 看板告警 + 总管轻巡检 |
| 4 | 工单自动认领 | 推主拉兜底；拉通道分级自主权：高优自动开工、其余半自动 |
| 5 | 工单拆分 | `ticket-split` skill 四条自检清单 + **父单方案**追踪需求闭环 |
| 6 | worklog 归档 | 按自然年切档，冷热分层，生成器全量扫描保证历史可见 |
| 7 | skill vs 规章制度 | "怎么干"→技能，"必须干"→制度；三档建立时机（私有/公司级/制度） |
| 8 | 团队统筹权 | roster 加 lead 列，团队负责人统筹团队 workflow 与技能 |
| 9 | 目录结构说明书 | 独立《目录结构说明书.md》+ company.md 引用 + `--check-structure` 自检 |
| 10 | 建司 skill | OPC 根 `create-company` skill + `company-template/`，交互式问卷建司 |
| 11 | 双入口效力边界 | 分级效令 + **领地自治**：业务直干、领地内自治、跨领地/公共领地经总管 |
| 12 | 入口 B 建单取号 | 预留号池自助取号（总管维护 reserved 号段，员工取号即登记占用） |
| 13 | 知识库定位 | 一套目录三分职能：common/ 背景资料 + methods/ 候选方法论中转区 + 项目知识归各项目 knowledge/（跨项目价值由总管提炼升级进 common） |
| 14 | 总管落位赋能 | 新建总管私有技能 `mechanism-sop`：四类落位决策树 + 领地表 + 知识库三分边界/提炼规则；建司时进模板 |
| 15 | 技能岗位定制 | 三档模式：纯引用 → 增量扩展（私有差异技能）→ 真分叉新建（登记 fork、不再自动跟进）；禁止"复制整份再改"的中间态 |
| 16 | 制度 vs 技能判定 | 技能=能力（做了更好），制度=义务（必须/禁止/时限/追责/适用范围）；总管判定口诀 + 三种落位组合进 mechanism-sop |
| 17 | 实体命名解耦 | 实体目录名 = `{前缀}{ID}`（ID-only）；显示名唯一真相：员工=roster「岗位」列，团队/项目=team.md/project.md「名称」字段——改显示名零文件系统波及；门禁新增裸路径散文扫描（--check）|

---

## 一、需求分析师（决策 #1）

**落点**：总管私有技能 `E0000-AI员工-总管/skills/demand-clarify/`。

- **流程**：接用户原始想法 → 一问一答澄清（一次一个焦点）→ 产出需求文档（PRD：背景/目标/范围/验收标准）→ 需求确认后存档 → 交 ticket-split 进入拆单。
- **升级预留**：roster 登记规范中预留"需求分析师"岗位定义；升级触发条件 = 并行需求 ≥2 个、或用户要求对话入口与调度分离。届时按员工模板复制建 E00xx，skill 内容平移为人设职责，零返工。

## 二、并发会话机制（决策 #2）

**核心**：一个会话 = 一个工位（seat），工位文件化，冲突面收敛为"谁有权改状态"。

新增公司级技能 `skills/concurrent-work/SKILL.md`（触发词：并发/多开/工位/收口），员工 AGENTS.md 加引用行；启动流程加"开工先建工位卡"。

**1. 工位卡**：会话启动在 `workspace/sessions/seat-<HHMMSS>-<2位随机>.md` 声明：会话类型（工单/直聊/子代理）+ 当前工单号 + 状态（工作中/已收口）；收口时改"已收口"。动手前先扫 `sessions/` 知晓在岗分身。

**2. 多会话各干不同工单（主场景）**：一会话一工单；产物写各工单 `deliverables/`，草稿写 `sessions/<卡>/scratch/`。

**3. worklog 追加协议**：重读 → 补块 → 临时文件 → 原子替换（P12），写完自读校验，坏块即补（只增不删，可自愈）。

**4. memory 防冲突**：`MEMORY.md` 改为只读合并区；并发沉淀写 `memory/inbox/<工位卡>.md`；**收口动作 = 合并 inbox 分片进 MEMORY.md 后删除分片**，合并前重读。

**5. 多会话共干同一工单**：**持卡会话**唯一有权改该单 `task.md` 的 status/owner；协作者只写 `logs/` 与各自 scratch；收口时持卡会话合并进 `deliverables/`。

**6. 工单 + 直聊并行**：直聊会话不碰 `task.md` 与 MEMORY.md，只追加 worklog（type=直聊）+ 写 memory inbox。

**7. 总管侧多子代理并发**：TSK 编号只由总管**主会话**分配（task-index 台账"查→占→写"独占）；子代理只领已建好的单、不建单。

## 三、前置阻塞（决策 #3）

- **字段契约**（P14）：`task.md` frontmatter 增加单行 `blocked_by: [TSK00007, TSK00009]`。
- **校验**：generate_tasks.py 校验被阻塞单的存在性与状态；无效引用告警（P11 跳过+告警）。
- **解锁三件套**：
  1. **员工自查**（主）：ticket-system 规定开工前必须校验 `blocked_by` 全部 done，未满足则留 backlog；
  2. **看板告警**（兜底）：生成器检测"上游已 done 而下游仍阻塞"→ 看板黄条；
  3. **总管轻巡检**：总管启动流程加一条——只扫 blocked 工单，有解锁才通知下游，无则跳过。
- **看板**：渲染"🔒 阻塞中"标记与阻塞链。

## 四、工单自动认领（决策 #4）

**推通道（主）**：总管建单即填 `owner`；派发注入三件套（工单路径 + 验收标准 + 时限）；员工回执即认领 = worklog 建「计划中」条目（填 ticket）。**认领 ≠ 开工**：status 保持 backlog，动工才 in_progress（看板可区分"已接单未动工"积压）。

**拉通道（兜底）**：员工启动流程自检——扫 `owner=自己 且 status=backlog 且无自己 worklog 条目` 的工单 → 补认领。

**分级自主权**：`priority: 高` 的漏接单**自动开工**（改 in_progress 并执行）；中/低只补认领回执等指令。直接用现有"高"档做阈值，零枚举改动。

**核验**：生成器交叉核验 `owner 有主但 worklog 无条目` → 黄条"未认领"（与双账核验同套逻辑）。

## 五、工单拆分与需求闭环（决策 #5）

**`ticket-split` skill**（总管私有）：廖哥三原则 + 拆分自检清单（四项全过才准建单）：
1. **单一负责人**：一单一 owner，跨员工的活必须拆；
2. **独立可验收**：有明确验收标准与交付物；
3. **一会话粒度**：理想一单一会话一口气干完，明显超了继续拆；
4. **依赖显式化**：先后关系用 `inputs` + `blocked_by` 声明，不许口头约定。

**父单方案（需求闭环）**：
- 拆单前总管先建 `type: 需求` 的**父单**（存 PRD/总验收标准，owner=总管）；
- 子单 frontmatter 加一行 `parent: TSKxxxxx`；
- 流转铁律增补：子单全部 done → 父单才能 review→done；
- 看板：父单详情渲染子单进度条；type 枚举复用现有"需求"值，零新实体。

## 六、worklog 归档（决策 #6）

- **冷热分层**：`workspace/worklog.md`（热）+ `workspace/worklog-archive/worklog-<年份>.md`（冷，只增不改）。
- **触发**：**按自然年**——每年首次写 worklog 时，先把上一年度已完成条目整体切走；未完成条目留热文件继续跟踪。
- **切档动作**：整块剪切 + 热文件留归档指针注释；归档文件永久保留，不做二次归档。
- **wid 不变**：`W-YYYYMMDD-序号` 自带日期，天然跨档唯一。
- **历史可见**：生成器扫描范围扩为"热文件 + `worklog-archive/*.md` 全量"；mydesk 加"含归档"筛选，默认显示全部历史。归档动的是文件位置，不动数据本身（P4）。
- **执行者**：员工本人（worklog-discipline 技能增加归档节）；总管巡检兜底（发现上年记录仍在热文件即提醒）；生成器永不写回数据层。

## 七、skill 与规章制度的边界（决策 #7）

| 内容性质 | 落点 |
|---|---|
| 具体怎么操作（步骤/命令/模板/告警对照） | **skill**（自解释自包含，P9） |
| 必须遵守什么（约束/红线/追责/生效范围） | **规章制度**（声明式，执行标准引用技能） |

- **防双份真相纪律**：同一条规则，"怎么干"只在技能、"必须干"只在制度，制度对技能只放一行权威引用（现有《工单操作规范.md》即为标准范式）。
- **三档建立时机**：单人用 → 私有 skill；≥2 员工需要 → 公司级 skill；需正式约束/追责 → 规章制度文档（可同时配技能）。
- **总管的"培训"** = 按落实 SOP 把条款落到对应层级引用行/技能/制度，不存在人肉培训。

### 7.1 为什么必须保留制度层（决策 #16）

技能与制度效力性质不同，公司级 skill 无法替代制度：

1. **技能是能力，制度是义务**：技能触发词命中才加载，无触发场景的纪律（如"开工先跑保活""不删 messages 历史"）写成技能可能永远不被加载，纪律悬空；制度是常在约束，声明对谁、何时、违反怎么办。
2. **审计追责锚点**：生成器告警、看板核验、追责指认的是"违反《XX 规范》第 X 条"，不是"没用某个技能"。
3. **变更程序不同**：技能=能力演进（总管随时可改）；制度=组织意志变更（修订须注明原因与日期，如 PRINCIPLES 的修订规则）。
4. **读者不同**：技能主要给 Agent；制度同时是给用户/管理者的组织意志表达。

**总管判定口诀**：只是"做了更好"→ 只建技能；含"必须/禁止/时限/追责/适用范围"语义 → 建制度，若需操作细节支撑则技能同步建/复用，制度引用技能为权威。

**三种落位组合**：①纯流程无合规 → 只建技能；②纪律简单、操作已被现有技能覆盖 → 只建制度引用现有技能；③纪律+新操作 → 制度+技能成对建。标准范例：现有《工单操作规范.md》（要点+范围+红线+引用）+ ticket-system（全流程操作）。

### 7.2 技能岗位定制三档（决策 #15）

| 档 | 场景 | 做法 |
|---|---|---|
| ① 纯引用 | 公司版完全适用 | 一行引用（现状） |
| ② 增量扩展 | 在公司技能上**加**本岗位特有步骤/字段/红线，主体流程不变 | 新建私有增量技能，正文只写差异（"执行 ticket-system §3 时，本岗位额外要求…"）；引用行先公司技能后私有扩展 |
| ③ 真分叉新建 | 差异大到换流程（触发词冲突/主体步骤改掉/公司版不适用） | 新建**改名的新技能**，登记 fork 来源+基准版本；此后公司版升级**不自动跟进**，由领地人自行合并 |

- 判定口诀：**"加上我的"→②扩展；"换成我的"→③分叉；复制整份再改=伪装成②的③，禁止**。
- ③ 的分叉技能目录名与触发词必须与原技能错开，正文头登记 `fork 自 <技能> <版本>`。

## 八、三级 workflow 与团队统筹权（决策 #8）

| 层级 | 内容 | 撰写/统筹 |
|---|---|---|
| 公司级 | 协作协议：需求流入路径、员工↔员工交接（inputs/handoffs/blocked_by）、跨团队路由、升级路径、需求闭环（父单验收） | 总管 |
| 团队级 | 团队内配合：组内工单分配、互审规则、组内技能共享 | **团队负责人（lead）**，成员可提案、lead 定稿 |
| 员工级 | 个人多工单/多会话编排：工位卡、收口合并、priority 驱动顺序 | 员工本人 |

- **roster.md 增加一列「角色」**（lead/成员），每团队指定一名负责人（可兼多团队，如 E0002 兼 T001 lead）。
- team.md 增补 lead 声明（引用 roster，不另建真相）。

## 九、目录结构说明书（决策 #9）

- **形态**：公司根独立《目录结构说明书.md》，company.md 加一行引用。
- **内容**：目录树 + 每项（职责 / 维护者 / 红线）+ 全部新增机制契约（sessions/、worklog-archive/、blocked_by、parent 等）。
- **防漂移**：generate_tasks.py 加 `--check-structure` 自检——说明书声明的目录缺失 → 告警；出现未声明的一级目录 → 告警。
- **复用**：本说明书即建司模板的结构契约母本。

## 十、建司 skill（决策 #10）

```
E:\OPC\
├── create-company/SKILL.md          # 建司技能（触发词：建公司/新建公司/创建公司）
└── company-template/                # 公司模板（从 C001 抽象）
    ├── company.md + 目录结构说明书.md
    ├── E0000/            # 000 号初始总管（模板自带）
    ├── skills/（四件套：ticket-system / worklog-discipline / concurrent-work / demand-clarify→总管私有）
    ├── workbench/（生成器 + 看板）/ 公司规章制度/ / 公司知识库/ / templates/
```

- **交互式问卷建司**：公司 ID（C002 起全局递增）/名称/业务域/预装技能（默认四件套）/是否初始团队与岗位；答完一键生成。
- **000 号总管入职动作**：读说明书与 roster → 确认 junction 四件套 → 向用户报到 → 按 dispatch-sop 推进后续团队/员工搭建，把公司运转起来。
- **编号空间**：公司 ID 全局唯一；E/T/P/TSK 每公司内独立编号。
- **老公司不受影响**：新机制直接在 C001 落地，由现有 E0000 管理；create-company 只管未来新公司。

## 十一、双入口与领地自治（决策 #11、#12）

### 11.0 原则：入口自由，机制收敛

- **入口 A（常态）**：用户 → 总管 → 派活（含总管自营兜底）。
- **入口 B（直通）**：用户 → 直接指挥员工。
- 入口只决定"活从谁手上进来"，不决定"机制怎么走"——凡涉及工单、留痕、状态机，两个入口汇入同一套文件契约，杜绝两份真相。检验标准：每条机制都要回答"入口 B 走到这一步时谁触发、谁有权、留痕到哪"。

### 11.1 领地自治（P20 升级表述）

| 领地 | 自治人 | 领地内可自治（用户直接下指令即可改，留痕 + 可逆 P24） |
|---|---|---|
| 员工领地 `E00xx/` | 该员工 | 技能增删、机制调整、workflow、memory、目录组织 |
| 团队领地 `T00x/` | 团队负责人（lead） | 团队 workflow、团队级技能、notices、团队资料 |
| 项目领地 `P00xx/` | 项目负责人（`project.md` 增 `owner: E00xx` 声明） | 项目文档、项目 workflow、项目内结构 |
| 公司公共领地（company.md、目录结构说明书、公司级 skills/、workbench/、公司规章制度/、roster） | **总管独占**（最高权限，全公司文件全生命周期管理） | 员工/团队无论接到谁的指令，动公共领地一律转总管 |

- 总管虽有权动一切领地，但尊重自治：领地内的事领地人管，总管不代劳。
- **红线例外**：领地自治不覆盖"公司级制度引用行"——员工可往自己 AGENTS.md 加技能，不得摘除 ticket-system 等公司级引用；卸底座螺丝必须经总管。

### 11.2 入口 B 的四个缺口闭环

| 缺口 | 规则 |
|---|---|
| G1 建单权 | **预留号池自助取号**：总管每次会话在 task-index.md 维护预留号段（约 20 个，标记 reserved-for-claim）；入口 B 需要建单时员工自助取号 → 立即在台账登记占用（重读→占→原子写，防并发撞号）→ 建单。总管仍是台账守护者（巡检核对）。直聊小活不建单，worklog 直聊条目留痕 |
| G2 认领 | 入口 B 用户指令 = 授权，员工自填 `owner` + 补 handoffs 单行 JSON（reason 注明"用户直派"）即完成认领 |
| G3 越权 | 分级效令 + 领地表（11.1）：业务性指令直干；跨领地/公共领地操作员工收到指令先上报转总管 |
| G4 汇报回路 | 入口 B 开工的工单，员工在 messages.md 追加"用户直派"来源备注（总管巡检可识别）；完成照常走状态机 + worklog，总管看板被动可见 |

### 11.3 总管自营单（总管被派活给自己）

1. 照常走完整状态机：`owner: E0000` 上板，不留暗账；
2. 身份切换留痕：执行时工位卡标注"执行身份"，调度义务（巡检/监控）不因自营而停摆；
3. **不自审自批**：自营单 review 的验收人 = 用户或总管指定的员工。

---

## 十二、知识库三分与总管落位赋能（决策 #13、#14）

### 12.1 公司知识库：一套目录，三分职能

```
公司知识库/
├── KB.md                    # 知识库总纲：三分定位 + 命名规范 + 提炼规则（总管维护）
├── common/                  # ① 背景资料库：行业资料/通用调研/踩坑案例——只供查，不指挥行为
└── methods/                 # ② 候选方法论（中转区）：员工经验落这里，等总管提炼
```

- **③ 项目知识延伸**归各项目 `P000x/knowledge/`（项目领地自治）；公司知识库存副本必漂移，跨项目复用价值由总管**提炼升级**进 common/（提炼观点，不复制文件）。
- **三分边界**（写进 KB.md）：教 Agent 怎么干→技能；约束 Agent 必须干什么→制度；给人/Agent 查的背景→common/；员工只对自己有用的经验→自己 memory/；对公司别人也有用的经验→methods/ 知识条目。
- **经验→方法论中转流**：员工提交 methods/ → 总管巡检顺带评审（并入现有轻巡检）→ 三选一：提炼成技能 / 提炼成制度 / 转正进 common；重复过时条目留痕归档。
- **与既有沉淀的分工**：memory=私人记忆；worklog=工作留痕；知识库=对公司/他人有用的显性知识。

### 12.2 总管落位赋能：mechanism-sop 技能

总管接用户构想 → 判定四类落位（决策树）：
1. **新实体** → 新目录 + 实体卡 + 登记（roster / task-index / parent 挂接）；
2. **新操作知识** → skill，按"谁需要"定层（1 人私有 / ≥2 人公司级 / 团队内团队级），登记引用行或 INDEX；
3. **新约束** → 规章制度文档，按条款判定范围落实引用行；
4. **新数据事实** → 数据层扩展三步：A 层字段契约（P14 预留）→ B 层解析校验（P11 跳过+告警）→ C 层展示；功能从数据层长出，不从看板硬编码。

配套：新视图 → page-templates 复制、只读投影、永不写回；领地表（§11.1）判定改动权限；KB 三分边界判定知识去处。

**文档分工**：《目录结构说明书》= 静态契约（是什么、谁维护、红线）；`mechanism-sop` = 动态流程（怎么改、怎么登记、怎么防漂移）。两者互补，总管接构想只查 mechanism-sop，落位结果对着说明书自检（`--check-structure`）。

---

## 十三、实施清单（按依赖排序）

| 批次 | 动作 | 涉及 |
|---|---|---|
| 1 | ticket-system 增补：blocked_by / parent / 认领双通道 / 拉通道分级自主权 / **预留号池自助取号 / 用户直派认领与来源留痕** | `skills/ticket-system/SKILL.md` |
| 2 | 新建 concurrent-work 公司级技能；员工 AGENTS.md 加引用行 + 启动流程建工位卡；worklog-discipline 增归档节；目录建 sessions/、worklog-archive/、memory/inbox/ | 公司 skills/ + 各员工目录 |
| 3 | dispatch-sop 增补：demand-clarify、ticket-split、编号主会话独占、总管轻巡检、父单创建、**预留号池维护**；**新建 mechanism-sop 技能** | `E0000-AI员工-总管/skills/dispatch-sop/SKILL.md` + 新建 demand-clarify / ticket-split / mechanism-sop |
| 4 | 生成器升级：blocked_by/parent 解析与告警、"未认领"核验、worklog 全量扫描（含归档）、`--check-structure`、**预留号段占用校验** | `workbench/generate_tasks.py`（B 层） |
| 5 | 看板升级：🔒 阻塞标记与阻塞链、父单子单进度、含归档筛选 | `kanban.html` / `mydesk.html`（C 层） |
| 6 | roster 加「角色」列；team.md 增 lead 声明；**project.md 增 owner 字段**；三级 workflow 文件落位 | roster.md / team.md / project.md / 三级 workflow.md |
| 7 | **知识库结构化：KB.md + common/ + methods/；员工模板与项目模板增 knowledge/ 占位** | 公司知识库/ / templates/ |
| 8 | 《目录结构说明书.md》定稿 + company.md 引用行 | 公司根 |
| 9 | OPC 根建 company-template/ 与 create-company skill（把 1–8 结论抽象进模板，000 号总管自带 mechanism-sop 等全部技能） | OPC 根 |

## 十四、2026-08-28 第二轮架构评审拍板记档

> 来源：`架构评审`（实读全部机制代码 + 自测实跑）后与用户一问一答确认。已实施项见对应提交；本节记档「已拍板待实施」与「待拍板」。

**已拍板并实施（本轮）**
- 自转通道 = **A+ 系统通知**：`opc_patrol` 发现异常弹系统通知（Win Toast / macOS / Linux notify-send），`opc.toml [patrol].notify` 可关；B 阶段「自动处置」扩展位 `[patrol].actor` 预留未启用。
- 跨平台验证 = **GitHub Actions 三平台矩阵**（`.github/workflows/ci.yml`）：sync-links → doctor → 6 selftest → 真实数据链路。
- 换行符纪律 = **`.gitattributes`**（B7）：`.sh/.py` 强制 LF、`.bat/.ps1` 强制 CRLF，行为随仓库走不随机器。
- 开公司前置修复：`create-company` 的 `home` 改指稳定锚 `companies/<cid>`（消除与 `opc-namespace-design.md` §2 的矛盾）+ 补 macOS/Linux junction 命令；`register-patrol` 任务名按公司隔离（`OPC-Patrol-<cid>`）+ 新增 `register-patrol.sh`。
- 看板公司名从 `company.md` 实体卡读取（原从目录名倒推，Z 方案锚路径下退化为裸 ID）；公司 ID 提取收敛为 `opc_resolver.extract_company_id` 公开 API（原 6 份副本 2 种语义）。
- 巡检 #8「知识库增量」机器化（2026-08-29 拍板）：心跳扫 `公司知识库/methods/` 与各项目 `knowledge/`，新增条目提示总管评审提炼；基线存 patrol-state `_meta`，首跑不告警。
- 裁撤（2026-08-29 拍板）：`scripts/watch-companies.py`（watchdog 依赖违反 P13 且从未启用，ensure-links + doctor 已覆盖其职责）；`SYSTEM_GUIDE.html` 因快照漂移同批裁撤——**后经用户拍板于同日重建**：按当前机制全量重写对外总览 `SYSTEM_GUIDE.html`，并新增面向小白的用户手册 `USER_GUIDE.html`（派活/对话/机制地图/命令速查/FAQ），两份均为手工维护快照，内文注明「以 README/PRINCIPLES 为准」。
- 编号原子分配（B5 落地，2026-08-29 拍板）：`opc_tickets --new --auto-id` 原子取号（扫最大已用号+1，目录存在性即锁，并发败者自动取下一号）；显式取号同样自动在 task-index「工单登记」占行（取号+占台账+建单一步）。
- 非法状态上板标红（P11 补齐，2026-08-29 拍板）：status 非法的工单不再整单跳过，保留上板（payload 增 `status_valid` 标记），kanban 对未知状态独立成列并回退红色渲染。
- 代码去重（D1/D2/D3 落地，2026-08-29）：`resolve_company` 收敛三份 resolve_ctx；`normalize_dt/normalize_day/read_text_warn/atomic_write` 收敛进 opc_model 共享，tickets/dashboards 删本地副本。

**已拍板待实施**
- （无——本节原列四项已全部落地，见下）

**第二轮「已拍板待实施」→ 已实施（2026-08-28 晚，第三批）**
- patrol findings 结构化（dict：no/kind/severity/action/ref/owner/msg，元数据唯一真相 `opc_schema.PATROL_CHECKS`）+ `patrol-state.json` 闭环态（log 审计只增不删 / state 可改，handled→再犯自动 reopened）+ `patrol-pending.md` open 态快照（总管启动第 5 步读它并置 handled）。
- `opc_model.py --list-skills` / `--sync-index`：技能元数据唯一真相在各 SKILL.md frontmatter（25 份已迁移补 triggers/summary 结构化字段），9 份 `skills/INDEX.md` 全部转生成物（不入库、勿手改）；PRINCIPLES P7 / AGENT_ECOSYSTEM §2.4 / 各 AGENTS.md / dispatch-sop 口径已同步，「登记」动作消失。
- `opc_resolver.py --ensure-links`：技能披露 junction 纳入 resolver 统一托管（manifest `[platform.workbuddy]` 配置化，层自动发现），`--sync-links` 保留为别名；doctor 第 5 项「OS 级链接完整性」（error 阻断）——根治「门禁假绿」（原断点 1 闭环，负例实测：删链接→doctor 阻断→ensure-links 重建→全绿）。
- `opc_resolver.py --diff-template`：C001 ↔ 模板双向 diff（忽略换行符与 `<CID>`/`<本司ID>`/`C00x` 占位符），当前 4 处差异均为 Q5 确认的有意保留（AGENTS.md 启动门禁节 / company.md 实体清单 / workflow.md 路径说明 / 说明书标题）。

**已拍板并实施（2026-08-29，原「待拍板」两档全部落地）**
- 并发写锁原语：`opc_model.file_lock`（O_EXCL 锁文件 + 忙等超时 + 过期锁抢占，纯标准库）；机制层写路径全部接入——建账 `locked_update`（wid 序号分配持锁）、台账登记 `file_lock`；agent 侧提供 `opc_model.py --append`（锁内追加），concurrent-work 技能已写明用法。`--new --auto-id` 的目录存在性即锁与本原语互补。
- 实体类型注册表：`opc.toml [entity_types]` 为前缀唯一真相（默认 E/T/P），`resolver.entity_types()` 统一供给 resolve_entity / dashboards.scan_entity_dirs / tickets.build_registry / check_structure / patrol 员工扫描——加新实体类型 = manifest 加一行，零代码改动；[company.DEFAULT] 中 teams/projects/employees 三个未消费死键移除。
- 心跳提频 + 通知去重（2026-08-29 拍板「每 30 分钟+通知去重」）：① 心跳节奏 每日 09:00 → 每 30 分钟（schtasks /SC MINUTE、cron */30；`register_heartbeat(every=30)` 默认，`--at` 保留为每日兼容；register-patrol 薄壳/README/USER_GUIDE/总管启动流程同步）；② 前置安全件 `new_findings` 通知去重——只对「新出现」的 finding 弹系统通知，open 态旧待办不重复打扰（pending 快照始终可见，处置后再犯自动重报）。浏览器按钮直驱 Python 的两条桥（自定义协议/本地服务）评估后不建：数据新鲜度在源头由心跳解决，按钮直驱留待 B 阶段本地 agent 服务顺带实现。
- 看板数据自愈 + 陈旧阈值语义修正（2026-08-29，用户实测反馈「点同步后陈旧横幅不消失」）：根因=浏览器「同步」只能重读数据文件跑不了生成器，而心跳只发现陈旧不刷新数据。修复：① `opc_patrol.auto_refresh`——数据缺失/陈旧（> `opc_schema.PATROL.regen_stale_minutes`=30 分钟）时心跳自动重生成（dry-run 跳过，失败降级 #10 告警）；② 六份看板模板陈旧阈值默认 10 分钟 → 1560 分钟（26 小时，匹配心跳日更节奏；watcher 实时模式可调回小值，localStorage key 升级 v2），横幅文案改为「心跳会自动刷新，点同步即可取到新数据」。效果：数据由心跳保鲜，横幅只在心跳真死了才出现，且点同步即消失。
- 车道判定口诀（2026-08-29 拍板：验收判据）：「要验收的活建工单，干完就完的直聊；拿不准就建单」写入 ticket-system 技能开头（双车道早有实现：worklog 四型/直聊不填 ticket/核验仅对工单型要求 ticket，但判定条款此前缺文）——补齐员工第一反应的口径不一致缝隙；USER_GUIDE FAQ/机制卡同步。
- 全自举（2026-08-29 拍板「新电脑 clone 后系统自己完成初始化」）：`opc_resolver.py --bootstrap` 一条命令完成链接+钩子+看板数据+技能索引+**公司心跳注册**（Windows schtasks / crontab，定时逻辑唯一来源收编 `opc_patrol.register_heartbeat`，register-patrol.{ps1,sh} 退化为薄壳）；`--doctor` 升级为「先自愈再检查」（安全幂等项自动补，打印 [fix] 行）——agent 按门禁跑 doctor 即完成自举。心跳是 bootstrap 唯一机器级副作用，`--no-heartbeat` 可关。Windows 本机实跑通过（schtasks 任务 OPC-Patrol-C001 就绪）；README「系统初始化」改写为新电脑三步。

## 十五、实体显示名与物理路径解耦（决策 #17，2026-08-29）

> 来源：E0001「分析员→售前工程师」改名实战复盘。改名执行数小时、逐文件手术，暴露「改名零改动」承诺只覆盖公司级（稳定锚 + `--ensure-links`），实体层无通道——显示名烧进物理目录名，改显示名=改物理路径=全量引用手术；且散文目录名漂移对 `--check` 隐形（只扫 opc:// URI），doctor 全绿但根文档 4 处旧名残留。

**演进记档（两轮，第二轮为终版）**：
- 第一轮（已否决）：实体目录裸 ID 化（`E0001/`），显示名唯一真相进 roster 岗位列。用户否决——**目录必须自解释**（打开仓库即知谁是谁），纯编号目录不可接受。
- 终版（实施）：**保留自解释目录名 `{前缀}{序号}-{名称}` + 把改名做成一条命令**。「零改动」的正确解读：引用层零改动（机制保证），机械动作零人工（命令包办）。

**终版决策**：
1. **目录名为显示名真相**：`{前缀}{序号}-{名称}` 目录自解释；roster「岗位」列须与目录一致，不一致生成器告警（目录名为准）。
2. **引用层 = ID 逻辑锚**：`opc://` URI 与看板/实体卡发现按 ID 前缀扫描（`{ID}-` 或精确 `{ID}`），物理目录改名天然透明，零动作自愈——这就是公司级稳定锚在实体层的等价物（实体无需 OS 锚树：`companies/<cid>` 是 junction 不可再嵌套，且 ID 扫描已覆盖其收益）。
3. **机械层 = 一条命令**：`--rename-entity <ID> <新名称>` 一条龙——git mv → 重建稳定锚/技能披露链接 → 同步 roster 路径列 → 重跑看板 → **全文旧目录名按 ID 映射自动改写** → 门禁终检。用户手动在文件管理器改名后，跑 `--heal-entity-refs` 同样一键自愈；心跳巡检（每 30 分钟）也自动重建断链。
4. **历史留痕区豁免**：worklog / memory / archive 里的旧目录名只报不改——旧名是历史事实（「只增不删」纪律）。
5. **门禁补盲**：`--check` 新增裸路径散文扫描——正文 `{前缀}+数字-名称` 必须匹配现存目录全名，否则报失效并提示一键自愈命令（历史区豁免）。改名后的散文漂移由机器拦截，不再依赖人眼逐文件翻。
6. **roster 发现自愈**：manifest roster 键失效时按总管 ID（`{员工前缀}0000`）前缀扫描兜底——机制自身不再有写死的实体物理路径。
7. **多公司同 ID 兼容（2026-08-29 补强）**：同 ID 实体分属多家公司是合法形态（公司目录天然隔离，引用层按 `opc://company:<cid>/...` 天然区分）。旧名自动改写按「行内锚前缀（companies/<cid>/）> 文件所在公司 > 全局唯一」四级归司，仍定不了拒绝猜测、报人工给出候选——绝不跨公司误改；`--rename-entity` 显式携带旧名→新名映射 + `--company` 消歧；roster 路径列各公司各同步各的；自愈重生成覆盖全部公司。回归用例进 `--selftest`（case 11）。

**落地物**：opc_resolver.py（`scan_stale_dir_refs` / `rewrite_stale_entity_names` / `_sync_roster_paths` / `heal_entity_refs` / `rename_entity` / roster_abs 自愈）、opc_dashboards.py（目录名为准 + roster 一致性告警）、opc_tickets.py（registry 目录名为准）、opc_patrol.py（心跳链接自愈 + 正则放宽）、dispatch-sop「员工改名 SOP」、41 份文档口径。


---
*定稿：2026-08-28 · 一问一答 17 项决策全记录（含双入口领地自治、知识库三分、总管落位赋能、技能定制三档、制度判定法则与实体命名解耦）+ 第二轮评审拍板记档（§十四）+ OPC 服务常驻中枢（§十六，决策 #18，2026-08-29）*


## 十六、OPC 服务：常驻后台中枢（决策 #18，2026-08-29）

**缘起**：TSK00010 关单后看板仍显「待审」——根因是纯静态 file:// 页面的「同步」按钮只能重读数据文件、跑不了生成器；watcher cmd 窗口易关、30 分钟心跳粒度太粗。用户拍板：点「同步」必须真重算，自动刷新必须真生效。

**十二问拷问后的共识**（一问一答全过程口头拍板）：
- 定位：**OPC 后台中枢**——机器唯一常驻进程，承载一切「必须有进程才能实现」的机制；文件仍是唯一真相与接口，进程无写业务数据的权力（只读 API + 触发重算，无任何写接口）。
- 定时任务类不做（agent 平台自带调度）。
- 架构：**进程=宿主，公司=租户**——扫描 opc.toml 全部公司逐家装载服务实例，新公司自动纳管；URL 按公司路由 `/{CID}/dashboard.html`，无前缀旧地址 302/落默认公司兼容。
- 通知：事件=**风险预警（#11 已逾期/截止日≤3天、#12 in_progress 超 3 天无更新，opc_schema.PATROL 可配）+ 巡检异常实时报**；节奏=新风险即时报 + 每日 09:00 未处理项汇总重提；通道可插拔（windows 内置，将来 agent 平台/webhook 配置扩展）。
- 心跳整体并入进程并**彻底清理遗留**：删 OPC-Patrol-* 计划任务、register-patrol 薄壳退役、doctor/bootstrap 改「OPC 服务探活」口径、run_boards 降级为手动应急工具。

**落地**：OPC 根新增 `opc_service.py`（opc_board_server.py 升级并入）；`opc_patrol.py` 重构——新增 #11/#12 风险检查、抽 `run_once()` 进程内复用（CLI 手动入口保留）、心跳注册函数删除；13 个看板页面「同步」按钮改调 `{相对深度}api/sync`；自启项=启动文件夹 `OPC-Service.vbs`（pythonw headless，免管理员，删文件即卸载）。


---

## 十七、三级知识库机制（决策 #19，2026-08-30）

**缘起（痛点优先）**：知识库只有公司级一个点（`公司知识库/` = common + methods 二分），团队级为零、员工级只有 `memory/`。后果三件：员工学到的东西要么烂在自己 memory 里别人看不到，要么塞进 `methods/` 等总管「三选一」提炼（单向且慢）；agent 想查一条知识只能全目录翻找，token 全花在「找」而不是「用」上；知识存进去就没人管，过期、重复、失焦三重腐烂，半年后没人敢信。

**拍板清单**（2026-08-30 一问一答，八问全记录）：

| # | 议题 | 结论 |
|---|---|---|
| 1 | 上浮形态 | **搬家 + 存根**（唯一形态） |
| 2 | 上浮准入 | 脱离原作者仍成立 + 跨 N≥2 角色有用；审批前查重 |
| 3 | 资讯共享 | **单开「登记」通道**，与上浮并列两条独立通道 |
| 4 | 员工级形状 | 新建 `knowledge/`，与 `memory/` 分工 |
| 5 | 三级结构 | 同构：`KB.md`（人写）+ `INDEX.md`（生成物）+ 主题子目录 |
| 6 | 加载 | 索引行 → 摘要 → 全文，逐级命中即停 |
| 7 | 覆盖与腐烂 | 三档可覆盖性 + 三条腐烂防护（合并汇报） |
| 8 | 命名 | 三级物理目录统一 `knowledge/`，逻辑名靠 manifest key |

**与既有「知识摄入与方法资产」的关系（两轴正交，勿混为一谈）**：PRINCIPLES 已拍板的「四级固化阶梯」（知识库条目 → 方法卡 → 技能/SOP → 机制/原则）是**硬度轴**——知识固化到什么程度；本节三级（员工 / 团队 / 公司）是**归属轴**——知识归谁管、谁能查。两条轴独立移动：

| 轴 | 移动方向 | 术语 | 谁批 | 动作 |
|---|---|---|---|---|
| 归属轴（本节） | 员工 → 团队 → 公司 | **上浮** | 本级管理员审批 | 物理搬家 + 原处存根 |
| 硬度轴（既有） | 条目 → 方法卡 → 技能 → 机制 | **晋级** | 按既有门槛（二次命中 / 定型有案例 / 规律稳定） | 改写形态，不是搬目录 |

> **为什么必须分开**：技能与知识库是两种形态。「上浮」若同时指两件事，半年后没人能推理一条知识「到底在哪」。

### 17.1 三级目录（同构是硬要求）

| 层 | 目录 | 管理者 | 可读范围 | 内容定位 |
|---|---|---|---|---|
| 员工级 | `E00xx/knowledge/` | 员工本人（可写） | 本人 | 本岗高频、个人方法论 |
| 团队级 | `T00x/knowledge/` | 团队 lead | 组内成员 | 组内共用、跨项目复用 |
| 公司级 | `knowledge/`（公司根） | 总管 | 全员 | 全公司复用、制度背景 |

每级同构 —— **同构不是美观问题**：agent 检索逻辑只写一份（P3/DRY），三级共用一套心智，上浮时零格式转换（搬家就是搬家，不是搬家加重写）。

```
{层}/knowledge/
├── KB.md           # 人写：本层组织原则、分类含义、准入标准（INDEX 生成不了的判断）
├── glossary.md     # 受控词表：术语 + 同义词 + 禁用词（tag 归一，防检索漏）
├── INDEX.md        # 生成物（opc_model --sync-index 产出，勿手改）
└── <主题>/         # 主题子目录
    └── <条目>.md
```

**状态不进目录名**：草稿/候选/正式/待复核/已归档/已废弃一律用 frontmatter `status` 声明（P5 引用制，不靠物理嵌套）。

### 17.2 条目字段契约

| 字段 | 谁填 | 用途 |
|---|---|---|
| `title` `type` `tags` | 人 | L0 索引行可见部分，**决定检索命中率** |
| `summary` | 人 | 摘要层（2-3 句）——不填则三级加载失效 |
| `status` | 人 | 生命周期状态（见上） |
| `author` `maintainer` | 人+审批 | 作者署名答疑、维护人管格式复核（**分离**：解决搬家后无人懂细节） |
| `review_by` | 人 | 复审到期日 = TTL（巡检 #14 提醒） |
| `derived_from` `supersedes` `source` | 人 | COW 衍生链 / 取代关系 / 登记型条目原文指针 |
| `hits` `tokens` | **机器** | 命中热度（LRU）+ 全文体积估算 |

`type` 取 **diátaxis 四分法**：`method`（怎么干）/ `reference`（查什么）/ `case`（案例与解释）/ `glossary`（术语）。与 P31 三问法对齐——换家公司仍成立的 `method` 才配往上浮。

### 17.3 三级加载（缓存行类比）

借鉴 CPU 缓存「按 64B 缓存行加载、不整块读」：**索引行常驻**（约 30 字符/条）→ 未命中即停 → **摘要**（命中才读）→ **全文**（确认有用才读）。查询顺序：自己 KB → 团队 KB → 公司 KB → 外部，**由近及远、命中即停**。

`tokens` 是 CPU 缓存没有、token 经济下必须有的一项：agent 在摘要层就看到「全文 1200 token」，据此判断是否值得读——等价于缓存行大小的作用。

### 17.4 上浮（唯一形态：搬家 + 存根）

**准入判据（两条同时成立）**：① 脱离原作者与原上下文仍成立；② 跨 N≥2 个角色有用（P31 最小公共上浮）。不满足的**挡回原层**——不是「换个方式上浮」，而是「不该上浮」。

**动作**：物理文件搬至上级 `knowledge/`，原处留**真实 `.md` 存根**（正文一句 `本文已上浮至 opc://…`，frontmatter 标 `status: 已上浮`）。

> **存根必须是真实文件，不是索引里的一行**：否则所有指向原路径的 `opc://` 引用全部断链。存根存在则引用层零感知、`check_links` 仍绿。

**审批三件事**：① 查重（索引里有无语义近似的，**有则合并不新增**）；② 作者/维护人分离登记；③ 定 `review_by`。

**为什么不是复制**：复制即漂移。知识库走 **exclusive cache**（一条知识物理上只存在一层），绝不做 inclusive（上层是下层副本）—— 这是 P2「引用代替复制」在知识层的落点。

### 17.5 登记通道（资讯共享，与上浮并列的第二条通道）

| | 上浮（promotion） | 登记（indexing） |
|---|---|---|
| 本质 | **归属转移** | 归属不动，只登记「结论 + 指针」 |
| 对象 | 内部知识（员工/团队产出） | 外部资料、长报告、存档 |
| 谁批 | 必须审批（改变领地） | 收录即可、管理员过目防垃圾（零风险） |
| 落地 | 物理搬家 + 存根 | `source` 字段指向原文，正文只写结论，`type: reference` |

登记型条目与本地条目**同索引、同加载路径**，只是没有本体。8000 字行业调研走登记 → 公司层只留 300 字结论；方法论走上浮 → 全文搬上来全员复用。

### 17.6 三档可覆盖性（OverlayFS COW 语义）

| 类型 | 能否覆盖 | 机制 |
|---|---|---|
| 制度 / 红线 | ❌ | 只能申请改源头，下级只读 |
| 方法论 / SOP | ⚠️ 可衍生不可覆盖 | COW 复制到本地做变体，强制标 `derived_from`，**源不变** |
| 背景资料 / 事实 | ✅ 可补充可纠错 | 本地写补充条目标 `source`/`supersedes`；同时发纠错提案 |

**纠错提案复用工单系统**（零新增机制）：一条「知识纠错」工单指向本级管理员，自动获得状态流转、派单、逾期/停滞预警与看板可见性。

**冲突发现**：`derived_from` / `supersedes` 让机器能画出知识衍生树；同一主题上下级都有条目却无关联标记 → 巡检判疑似冲突告警 → **人工裁决**（P29）。

### 17.7 腐烂防护（三条巡检，合并汇报）

| 腐烂 | 表现 | 机制 | 巡检项 |
|---|---|---|---|
| 过期 | 事实变了条目没变（**最危险**：agent 会拿着过期知识干活） | `review_by` 到期提醒 | #14 |
| 重复 | 同一件事 N 个版本并存 | 上浮查重 + 疑似重复扫描 | #15 |
| 失焦 | 长期零命中仍占索引 | `hits` 为零 → 提醒归档 | #16 |

**铁律：永不自动删除，只降级与归档**（P24 可逆 + P28 审批门）。（巡检编号自 #14 起：#13 已被依赖环检测占用——PATROL_CHECKS 现有 13 项，2026-08-30 顺延修正）CPU 的 eviction 是硬件自动、无损淘汰（主存有副本）；知识淘汰必须人工决策且可逆。

**噪声控制**：三条全部并入每日 09:00 摘要，不实时弹窗（critical 除外），阈值在 `opc.toml [knowledge]` 可调。

### 17.8 变更感知（用户手动改目录如何被感知）

**关键解耦：agent 不需要「感知变更」，只需要「每次读到最新的索引」。** 索引永远新鲜，agent 开工读到的就是当前全貌——它要的是现状，不是历史。

| 对象 | 机制 |
|---|---|
| agent | 服务监听 `knowledge/` → 实时重生成 `INDEX.md`；**开工自检兜底**（一次 `--sync-index`，幂等毫秒级，服务不在也不瞎） |
| 人（含用户） | diff 写 `KB-CHANGELOG.md`（新增/删除/改名/移动/状态变更） |
| 通知 | **分层**：改哪级通知哪级管理员 + 订阅该主题者，并入实时通知与每日摘要 |
| 文档漂移 | **不进自动巡检**（2026-08-30 实测后撤回）：KB.md 是三级知识库里唯一人写的部分，也是唯一会漂移的部分，但它是**自由文本**——含反例（`草稿/` `已归档/` 是「不许这么建」的反面示例）与兄弟目录（`memory/`），正则提取实测 **28 条全误报、0 真阳**，自动告警只会淹看板（P29 教训）。**正确解法就是上面三行**：目录变动 → INDEX 重生成 → CHANGELOG 留痕 → 分层通知管理员 → 人自己判断要不要更新 KB.md。主动排查入口 `python opc_model.py --kb-drift`（机器给工具，人决定何时用） |

### 17.9 URI 与配置

```
opc://company:C001/knowledge/<路径>                    # 公司级
opc://company:C001/team/T001/knowledge/<路径>           # 团队级
opc://company:C001/employee/E0002/knowledge/<路径>      # 员工级
```

沿用现有 `opc://<scope>/<type>/<id>/<sub>` 语法，**resolver 零改动**：公司级走 manifest key 透传（`knowledge = "knowledge"`），团队/员工级走实体目录 + sub 逐级校验（已支持）。

`opc.toml` 新增 `[knowledge]` 段：`review_warn_days`（复审提前提醒天数）/ `dupe_similarity`（疑似重复阈值）/ `cold_hits_days`（零命中归档阈值）。默认保守——宁可少报，不可刷屏。

### 17.10 边界（与既有机制的分工）

| 内容性质 | 去处 |
|---|---|
| 教 agent 怎么干（可执行流程） | 技能 `skills/`（知识库不放） |
| 必须干什么（约束/红线） | 规章制度（知识库不放） |
| 时序私人记忆（日志、细节优先） | 员工 `memory/`（**不进索引、不上浮**） |
| 结构化可复用知识 | 三级 `knowledge/`（进索引、可上浮） |
| 外部资料 / 长报告结论 | 登记通道（只存结论 + `source` 指针） |

### 17.11 落地物

| 文件 | 改动 |
|---|---|
| `opc.toml` | `knowledge = "knowledge"`（值随目录改名）+ `[knowledge]` 配置段 |
| `opc_model.py` | 新增 `sync_kb_index()`：三级 INDEX 生成 + 字段校验 + 词表归一；`--sync-index` 一并调用 |
| `opc_patrol.py` | 新增巡检 #14/#15/#16；`_count_knowledge` 改三级扫描 |
| `opc_service.py` | `_sources_mtime` 纳入 knowledge 目录；变动 → 重生成索引 + CHANGELOG + 分层通知 |
| `opc_resolver.py` | 零改动（URI 层已支持）；doctor 增知识库结构检查 |
| 目录 | C001 / E0000 总管 / company-template 同步建三级 `knowledge/` |

---
*增补：2026-08-30 三级知识库机制（决策 #19）——三级同构、上浮搬家存根、登记通道、三级加载、三档覆盖、腐烂防护三条、变更感知分层通知*
