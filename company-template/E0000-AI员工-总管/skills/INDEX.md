# 员工私有技能 · 披露索引（INDEX）

> **本文件是 E0000（总管）私有技能（`skills/`）的披露索引，实现"渐进式披露"**：
> 遇到任务**先查本索引**（触发词摘要+路径，几 KB）→ 判断命中哪个技能 → **命中才读对应 `SKILL.md` 全文**。
> 未命中不加载任何技能正文——跨目录派活/子 Agent 场景下省 token 的关键通道（平台披露覆盖不到员工私有技能，实测确认）。
>
> **登记规范**：新增/修改私有技能时，必须同步更新本索引一行，保持披露层与正文一致。

---

| 技能 | 触发词（命中即加载） | 摘要 | 路径 |
|---|---|---|---|
| **dispatch-sop** 派发 | 派任务、派发、分配、接需求、调度、建工单、创建任务 | 总管接到需求后：拆解 → `--new` 建单 → 台账登记 → 选人 → 派发（注入 AGENTS.md+workflow+memory，禁注入技能全文）→ 看板监控 | `skills/dispatch-sop/SKILL.md` |
| **status-logging** 状态留痕 | 更新状态、记一下、留痕、看板、状态变更 | 状态留痕规范：员工改 `task.md` 即留痕（看板自动呈现），状态可追溯 | `skills/status-logging/SKILL.md` |
| **demand-clarify** 需求澄清 | 需求、想法、构想、我想要、帮我理、澄清、PRD、需求分析、立项 | 需求分析师流程：复述理解 → 顾虑编号 → 一问一答澄清 → PRD 存档（workbench/REQ-*.md 或父单正文）→ 交 ticket-split 拆单 | `skills/demand-clarify/SKILL.md` |
| **ticket-split** 工单拆分 | 拆单、拆分、拆需求、分解任务、怎么拆、工单拆分、需求闭环 | 三条拆分原则 + 四项自检（单一负责人/独立可验收/一会话粒度/依赖显式化）+ 父单闭环结构建单 | `skills/ticket-split/SKILL.md` |
| **mechanism-sop** 机制落位 | 机制、制度、放哪里、建目录、落位、扩展、怎么改、设计、搭建、升级机制 | 总管核心赋能：四类落位决策树（实体/技能/制度/数据/视图）+ 领地表 + 知识库三分 + 双入口检验 + 执行五步（清单→过目→备份→执行→验证） | `skills/mechanism-sop/SKILL.md` |

---

## 使用流程（给子 Agent / 调用方）

1. 读本索引，按触发词判断任务命中哪个技能。
2. 命中 → 打开对应 `SKILL.md` 全文，按流程执行。
3. 未命中 → 不加载技能正文，按常规流程处理。

*创建：2026-08-27 · 配套 PRINCIPLES P3（功能单一不耦合）/ P7（引用与身份规范分离）/ P9（自解释）*
