# 员工私有技能 · 披露索引（INDEX）

> **本文件是 E00x（【岗位名】）私有技能（`skills/`）的披露索引，实现"渐进式披露"**：
> 遇到任务**先查本索引**（触发词摘要+路径，几 KB）→ 判断命中哪个技能 → **命中才读对应 `SKILL.md` 全文**。
> 未命中不加载任何技能正文——跨目录派活/子 Agent 场景下省 token 的关键通道（平台披露覆盖不到员工私有技能，实测确认）。
>
> **登记规范**：新增/修改私有技能时，必须同步更新本索引一行，保持披露层与正文一致。

---

| 技能 | 触发词（命中即加载） | 摘要 | 路径 |
|---|---|---|---|
| 【技能名】 | 【触发词，逗号分隔】 | 【一句话摘要】 | `skills/<技能名>/SKILL.md` |

---

## 使用流程（给子 Agent / 调用方）

1. 读本索引，按触发词判断任务命中哪个技能。
2. 命中 → 打开对应 `SKILL.md` 全文，按流程执行。
3. 未命中 → 不加载技能正文，按常规流程处理。

*配套 PRINCIPLES P3（功能单一不耦合）/ P7（引用与身份规范分离）/ P9（自解释）*
