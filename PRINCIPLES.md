# 组织级设计原则（PRINCIPLES）

> **定位**：OPC 组织层通用原则库。适用于本目录下所有公司、项目、系统的设计与搭建。
> **使用方式**：搭建任何新东西（新公司 / 新项目 / 新系统 / 新技能）**前**，先通读本文件；具体落地差异写在各公司 `company.md` 或项目文档中，本文件只写通用原则。
> **维护**：原则来自实际协作中的经验沉淀（含踩坑教训），逐条编号，可追加、可修订，但**修订需注明原因与日期**。
>
> 文中示例以 `opc://company:C001`（C001-AI自动化公司 实例）为参照，**示例仅供参考，原则才是规则**。

---

## 一、架构哲学

- **P1 · 文件系统即真相**：领域事实只存在于文件系统，一切视图（看板/报表/界面）都只是它的投影。投影只读、可重建，真相永不丢失。
  - 示例：C001 工单系统 —— `tasks/` 目录是唯一真相，看板 HTML 是投影。
- **P2 · 单一真相**：任何事实只允许一个权威出处。引用（指针/路径/声明）代替复制；确有主从时明确定主（主从划分），防止两份真相漂移。
  - 示例：工单归属用 `project: P001` 声明而非物理嵌套；目录扫描为主、工单声明兜底。
- **P3 · 高内聚低耦合 · 功能单一不耦合**：每个模块/文件/技能只干一件事，边界清晰；模块间只通过**明确定义的接口**协作，不越界、不互相依赖实现细节。
  - 示例：技能引用只放 AGENTS.md；workflow.md 不承担单点技能职责（见 P7/P8）。
- **P4 · 单向数据管道**：数据流 `数据源 → 生成层 → 展示层` 单向流动；展示层永不写回数据源；需要写回时另设后端（明示代价后再做）。
- **P5 · 实体即目录、引用制声明归属**：实体（项目/团队/员工/工单）各占一个目录，彼此归属关系用字段声明，不靠物理嵌套；支持多对多、可迁移、可重组。

## 二、技能体系

- **P6 · 三层技能隔离**：公司级（组织根 `skills/`，全员可用）/ 团队级（团队目录 `skills/`，团队内）/ 私有（个人目录 `skills/`，仅本人）。放哪层由"谁需要"决定，不重复建。实体一律只存 `{层}/skills/` 一处（单一真相）；**各平台如何让技能被自动识别/披露属平台接入细节，不写入通用原则**——如 WorkBuddy 用 `{层}/.workbuddy/skills` junction（工作区级渐进式披露），Codex/Claude Code 各按其平台机制，具体见 `AGENT_ECOSYSTEM.md`。
- **P7 · 技能引用与身份规范分离**：AGENTS.md 只写根本性规范（身份/职责/红线）与一行技能索引入口；技能引用统一走 `opc://company:<id>/skill/<名称>` 逻辑符号（见 opc-namespace-design.md），命中触发词再读 `SKILL.md` 全文——避免 AGENTS.md 膨胀，功能单一不耦合（P3）。技能元数据唯一真相在 `SKILL.md` frontmatter（name/description/**triggers**/**summary**）；`{层}/skills/INDEX.md` 是**生成物**（`python opc_model.py --sync-index` 产出，不入库勿手改），发现走 `--list-skills` 或读生成的 INDEX，「登记」这个动作消失。技能引用一律不进 workflow.md。
- **P8 · workflow.md 仅描述多技能编排/复杂协同任务**：只有当任务需要多个技能配合、流程复杂到单文件说不清时，才用 workflow.md 描述流程；简单任务不进。
- **P9 · 技能自解释自包含**：技能内容（SKILL.md）必须自带完整流程、模板、告警对照、红线，靠"一行引用"即可加载使用，**不依赖人肉培训**。
- **P10 · 规范文件层与平台技能的边界**：组织目录下的技能是"规范文件层"（靠 AGENTS.md 引用路径读取，不会自动触发）；平台注册的技能才自动触发。设计时要明确用的是哪种。**多 Agent 平台接入方法见 `AGENT_ECOSYSTEM.md`**（WorkBuddy / Claude Code / Codex 各自目录与披露机制）。

## 三、工程实践

- **P11 · 容错铁律：跳过 + 告警，不降级上板**：坏数据/非法数据 → 跳过并告警，**绝不静默降级**（如非法状态降级成默认值）污染结果；也不因单个坏数据一票否决整体。
- **P12 · 原子写**：所有产出文件先写临时文件再替换（`.tmp` + `os.replace`），避免"写一半被读"。
- **P13 · 零依赖、可移植**：交付物尽量零第三方依赖、双击即开、单文件可分发、可版本化；引入依赖前先问"值不值"。
- **P14 · 契约先行、预留接口、延迟决策**：先定数据契约/接口（Schema），再实现；一时用不上的能力先定义字段/接口占位（空着不报错），未来扩展零成本、不推倒重来。
- **P15 · 数据格式纪律**：日期统一规整（如 `YYYY-MM-DD`，补前导零，保证字典序=时间序）；结构化数据用单行 JSON；前端 JS 字符串**禁止字面 `</script>`**（转义为 `<\/script>`）。
- **P16 · 测试习惯：改必测、测必清**：每次改动跑自检（单元+集成+语法/DOM+模拟渲染验证）；测试数据用后即清，保持生产数据干净。

## 四、协作流程

- **P17 · 先调研汇报，再讨论方案，再执行**：动手前先摸清现状、汇报理解；方案经讨论确认后再落地，不闷头开干。
- **P18 · 需求一问一答澄清，决策先行存档**：需求模糊时逐问澄清、一次一个焦点；决策确认后先存档（PRD/文档）再构建，避免"边做边漂"。
- **P19 · 区分「流程偏好」与「正确性底线」**：流程偏好（命名/格式/顺序等）用户可随时覆盖；正确性底线（数据对错、规则遵循、安全边界）不可妥协。
- **P20 · 领地划分**：数据/内容维护者只管自己的目录；代码/系统改动由对应角色协作完成，使用者不越权改系统层文件。

## 五、红线底线

- **P21 · 禁止打补丁式修法，根治根因**：出问题先找根因再修，禁止"哪疼贴哪"；反对从一个极端跳到另一个极端，逐案分析、不模板化应付。
- **P22 · 多轮自审后才报完成；成果由用户自验**：AI 不自验流程、不凑数注水；用户是最终验收者，未经验证的事要明说"待你实测"。
- **P23 · 坦诚不确定性，严禁迎合改结论**：数据/结论不确定就直说并查证；不因用户质疑而盲目改口，保持逻辑一致。
- **P24 · 变更优先零风险可逆**：修改现有结构时优先选可逆方案（改名/禁用/备份而非删除），防止不可逆破坏。
- **P25 · 共享读取器（单一解析点）**：实体卡（task/project/team/worklog/affair 等）的 frontmatter 读取统一走 OPC 根 `opc_model.py` 的 `parse_frontmatter`，禁止各脚本私写解析正则（呼应 P3 高内聚/DRY）。读取器只把卡片翻译成 dict，不校验、不写死字段——加字段免费、改格式只动一处；改名类问题靠 consumer 一处修，不做"校验不过就拦下"的复杂机制。本规则对 C001 与 company-template 新建公司同时生效。
- **P26 · 全符号化/稳定锚化，禁止写死物理路径**：凡跨文件/跨目录的内部引用，一律走 `opc://` 逻辑符号（文档层，resolver 可校验）或 `companies/<cid>` 稳定锚（运行时层，浏览器/shell 可解析）。**禁止硬编码物理目录名**（如 `C001-AI自动化公司/`、`C001根/`）或绝对路径——改名/迁移后全量失效即违背符号化初心。稳定锚位于 **OPC 根（高于公司根一级）**，子目录相对链接须 `../../companies/<cid>/`（两级上跳到 OPC 根），深度用 `anchor_prefix(base_dir, subdir)` 自动算、不手填（详见 opc-namespace-design.md §6.1）。本规则对 C001 与 company-template 同时生效。

---

## 六、人机协作

> 本章三条来自 2026-08-29 对开源项目 **Multica**（github.com/multica-ai/multica，48k stars，人机混合团队管理平台）的调研——取其设计思路，不引入其实现（详见 `research/multica-调研报告.html`）。**三条均为规范声明，零代码改动、零新增操作步骤。**

- **P27 · 协调层不生产 Agent 能力（Runtime 中立）**：OPC 是协调层，不是执行层——它只提供命名、路由、发现、门禁，**不绑定任何 Agent CLI、模型或平台**。用哪个 Agent 干活是**配置**（roster.md / opc.toml），不是硬编码；换 Agent 是改一行配置，不是迁移。
  - 依据：Multica 的关键取舍——"It does not ship a model. It drives the agent CLIs you already have installed"。
  - 现状：2026-08-29 全仓勘查确认 `.py` 机制层**零 Agent 名硬编码**（仅 `.workbuddy` 等平台目录名，非选型耦合）。本条为**固化现状、防止未来退化**。
  - 判据：任何时候发现脚本/员工卡里写死了 `claude` / `codex` / `codebuddy` 等具体 CLI 名并据此分支，即违背本条。
- **P28 · 产出审批门（Review Gate）**：Agent 的产出**未经人类点头，不进主数据、不进主分支、不标记完成**。静态门禁（pre-commit 的 `--check`）只能验引用有效性与结构完整性，**不能替代人工验收**——链接全绿不等于活儿干对了。
  - 依据：Multica 的 Review gates——"Work lands in review, not in main. You decide what ships."
  - 落地：产出先落待审区/评审态，人类确认后再合入；**不自审自批**（与总管规范"验收=用户或指定员工"一致）。
- **P29 · 自动化的边界在「发现」，不在「代决策」**：周期性自动化（2026-08-29 决策 #18 起 = OPC 服务内置巡检，原 `opc_patrol.py` 心跳已退役）**只负责发现与留痕，处置决策保留人类在环**。自动派单 / 自动处置是**预留扩展位**，`opc.toml [patrol].actor` 已留接口但**默认不启用**——启用需显式拍板，不做默认。
  - 依据：Multica 的 Autopilots（cron 触发周期性工作）与它的教训——编排自动化一旦越界到"代决策"，失败模式会淹没看板（其 issue #815：用管人的抽象管 Agent，Agent 的失败模式与人根本不同）。
  - 判据：机器可以**提醒**"该开工了"，不可以**替人决定**"这就算做完了"。

---

## 附：原则索引速查

| 编号 | 一句话 |
|---|---|
| P1 | 文件系统即真相，视图只是投影 |
| P2 | 单一真相：引用不复制，主从明确 |
| P3 | 高内聚低耦合，功能单一不耦合 |
| P4 | 单向数据管道，展示层永不写回 |
| P5 | 实体即目录，归属用引用声明 |
| P6 | 技能三层：公司级/团队级/私有 |
| P7 | 技能引用只放 AGENTS.md |
| P8 | workflow.md 只描述多技能/复杂任务 |
| P9 | 技能自解释自包含，不人肉培训 |
| P10 | 规范文件层≠平台技能，边界要明示 |
| P11 | 容错：跳过+告警，不降级上板 |
| P12 | 原子写，防写一半被读 |
| P13 | 零依赖、可移植、可版本化 |
| P14 | 契约先行、预留接口、延迟决策 |
| P15 | 数据格式纪律（日期/单行JSON/转义） |
| P16 | 改必测、测必清 |
| P17 | 先调研汇报→讨论→再执行 |
| P18 | 需求一问一答，决策先行存档 |
| P19 | 流程偏好可覆盖，正确性底线不可妥协 |
| P20 | 领地划分，使用者不越权改系统 |
| P21 | 根治根因，不打补丁；逐案分析 |
| P22 | 多轮自审，成果用户自验 |
| P23 | 坦诚不确定，不迎合改结论 |
| P24 | 变更优先零风险可逆 |
| P25 | 共享读取器：实体卡读取统一走 opc_model.py，禁私写正则 |
| P26 | 全符号化/稳定锚化，禁止写死物理路径；深度用 anchor_prefix 算 |
| P27 | 协调层不生产 Agent 能力，选型是配置不是硬编码 |
| P28 | 产出审批门：未经人类点头不进主数据，静态门禁不替代验收 |
| P29 | 自动化边界在「发现」不在「代决策」，自动处置默认不启用 |
| P30 | 跨平台三系统：一切机制代码/流程设计必须 Windows、macOS、Linux 三系统可正常运行（廖哥明令，2026-08-29） |
| P31 | 内容分层归属与精简：三问法归位、引用代替复制、正文上限；写规范类内容「精准高效、本质而非肤浅」，事实记录类例外细节优先 |

## P30 跨平台三系统（2026-08-29，廖哥明令）

**一切机制代码与流程设计，必须保证 Windows、macOS、Linux 三系统可正常运行。** 不允许「先在 Windows 跑通、别的系统再说」。

落地纪律：
- **平台差异收口在机制层**：路径分隔符（os.path/os.sep，禁手拼 `/`、`\`）、换行符（.gitattributes 已定：.sh/.py 强制 LF、.bat/.ps1 强制 CRLF）、OS 链接（Windows junction / *nix symlink，收口 `opc_resolver._create_link`）、计划任务与自启（schtasks / crontab / 启动文件夹，收口 `opc_service.register_startup`）、控制台编码（reconfigure + latin-1 兜底）——差异只在机制层写 `sys.platform` 分支，consumer 永远不感知；
- **新增行为必须进 selftest**：selftest 是 CI 三平台矩阵（ubuntu/macos/windows × py3.11/3.13）的回归载体，凡是能用临时目录自测的行为（如多公司归司 case 11）都要进；
- **门禁即跨平台验收**：`--doctor` 全绿 + 全模块 selftest + CI 矩阵全绿，才算改完。


## P31 内容分层归属与表达精简（2026-08-29，廖哥拍板）

**内容归位只问三个问题（三问法）**：跨公司普遍成立 → 根层一份；本公司政策、多角色遵守 → 公司层正文，其余层**只留一行引用**（`opc://…` 链接 + 一句「为什么读」），禁止复制；仅单个角色执行 → 该员工技能正文。

**表达总纲（廖哥明令）**：总管、各员工、各会话新建或改动规范/原则/技能类文件内容时，一律「精准高效、本质而非肤浅」——每句话要么改变行为、要么帮助定位，不写铺垫与泛泛而谈。

**表达约束**：SKILL.md 正文是「路由器」不是百科（渐进式披露：正文 ≤120 行，细节拆 Level 3 附属文件按需读）；description/triggers 写「何时触发」，不复制内容摘要；同一规则全组织只写一份，其余引用。

**例外（细节优先）**：记忆、日志、worklog、messages 等事实与事件记录类内容，以保证信息细节不丢失为先，不受本条精简约束。

**机制保障**：doctor 门禁检测 SKILL.md 正文超限（机器发现、人工决策）；实例↔模板双份一致性由 `--diff-template` + `template-diff-allowlist.md` 管（打样先例：ticket-system 258→80 行 + reference/walkthrough 拆分）。


---
*创建：2026-08-27 · 修订：2026-08-28 增补 P26（全符号化/稳定锚化，廖哥明令"肯定全净化"）；2026-08-29 增补 P27~P29 人机协作三条（吸收 Multica 设计思路，调研见 `research/multica-调研报告.html`，零代码改动）及 P30 跨平台三系统（廖哥明令：所有修改与机制设计兼顾 Win/mac/Linux）；2026-08-29 增补 P31 内容分层归属与表达精简（三问法，ticket-system 打样）； · 来源：`opc://company:C001` 协作看板系统 20 轮迭代经验沉淀（含廖哥明令原则）*
